# Flop duplication through NetworkEdit, then a relink proves RTLIL kept it.
read_liberty -lib test/simple.lib
read_verilog test/simple.v
hierarchy -top top
opensta_net -liberty test/simple.lib -link
opensta_net -source test/dup.tcl
opensta_net -link
opensta_net -source test/dup_fresh.tcl
